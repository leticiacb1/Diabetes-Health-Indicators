name: Diabetes Health Indicators Workflow
on:
  push:
    branches:
      - main
jobs:
  preprocess:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - run: echo "⚙ Install code dependencies ..."
      - name: Install dependencies
        run: pip install -r requirements.txt

      - run: echo " 🎬 Run preprocess function ..."
      - name: Run preprocess function
        run: python -m src.preprocess

  tests:
    needs: preprocess
    runs-on: ubuntu-latest
    steps:
      - run: echo "📌 Run tests ..."
      - name: Run tests
        run: pytest